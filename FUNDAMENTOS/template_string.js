const nome = "helklen"

const concatenacao = nome + " borges"

console.log(concatenacao)

const template = `${nome} borges`

console.log( `10 + 15 = ${10 +15}` )

const caixa = texto => texto.toUpperCase()

console.log(`Ola ${caixa("fulano")}`)